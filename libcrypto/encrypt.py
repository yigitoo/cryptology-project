import re
import cv2 as cv
import PIL
import sys
import os
import math


def text2sound(dest = None, output = None) -> str:
    return f"The video has ben decrypted in {output} path."


def sound2video(dest = None, output = None) -> str:
    return f"The sound has ben decrypted in {output} path."
